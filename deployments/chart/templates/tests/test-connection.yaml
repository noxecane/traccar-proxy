apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "positions.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "positions.name" . }}
    helm.sh/chart: {{ include "positions.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "positions.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
